@extends('layouts.app')

@section('title', 'Edit {{ varName }}')

@section('content')
    <div class="max-w-xl mx-auto px-4 py-6">

        <h1 class="text-2xl font-bold text-gray-800 mb-6">
            Edit {{ varName }}
        </h1>

        <form method="POST"
            action="{{ route('{{ varName }}.update', ${{ varName }}) }}"
            class="bg-white shadow-md rounded-xl p-6 space-y-5">
            @csrf
            @method('PUT')

            {{-- Title --}}
            <div>
                <label for="title" class="block mb-2 font-semibold text-gray-700">
                    Title
                </label>
                <input type="text"
                    name="title"
                    value="{{ old('title', ${{ varName }}->title) }}"
                    class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-gray-900"
                    required>
            </div>

            {{-- Date --}}
            <div>
                <label for="date" class="block mb-2 font-semibold text-gray-700">
                    Date
                </label>
                <input type="date"
                    name="date"
                    value="{{ old('date', ${{ varName }}->date) }}"
                    class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-gray-900"
                    required>
            </div>

            {{-- Capacity --}}
            <div>
                <label for="capacity" class="block mb-2 font-semibold text-gray-700">
                    Capacity
                </label>
                <input type="number"
                    name="capacity"
                    value="{{ old('capacity', ${{ varName }}->capacity) }}"
                    class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-gray-900"
                    min="1"
                    required>
            </div>

            {{-- Actions --}}
            <div class="flex justify-between items-center pt-4">
                <a href="{{ route('{{ varName }}.index') }}"
                type="button"
                class="px-5 py-2  bg-gray-500 text-white rounded-lg hover:bg-gray-700 transition">
                    Back
                </a>

                <button type="submit"
                        class="px-5 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition">
                    Update
                </button>
            </div>
        </form>
    </div>
@endsection