@extends('layouts.app')

@section('title', 'Liste de {{ name }}s')

@section('content')
    <div class="max-w-5xl mx-auto px-4 py-6">
        <div class="flex justify-between items-center">
            <h1 class="text-2xl font-bold text-gray-800">
                Liste des {{ name }}s
            </h1>
            <a href="{{ route('{{ route }}s.create') }}"
                type="button"
            class="px-3 py-1.5 text-sm bg-green-500 text-white rounded hover:bg-green-600 transition">
            Créer un {{ name }}
            </a>
        </div>

        <x-alert/>

        @if (${{ varName }}s->count())
            <div class="mt-4 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                @foreach (${{ varName }}s as ${{ varName }})
                    <div class="bg-white rounded-xl shadow-md p-5 hover:shadow-lg transition">

                        {{ index_fields }}

                        <div class="flex gap-2 mt-4">
                            <a href="{{ route('{{ route }}s.show', ${{ varName }}) }}"
                            class="px-3 py-1.5 text-sm bg-blue-500 text-white rounded hover:bg-blue-600 transition">
                                Voir
                            </a>
                            @can('update', ${{ varName }})
                                <a href="{{ route('{{ route }}s.edit', ${{ varName }}) }}"
                                class="px-3 py-1.5 text-sm bg-yellow-500 text-white rounded hover:bg-yellow-600 transition">
                                    Éditer
                                </a>
                            @endcan

                            <form action="{{ route('{{ route }}s.destroy', ${{ varName }}) }}"
                                method="POST"
                                onsubmit="return confirm('Supprimer cet {{ varName }} ?')">
                                @csrf
                                @method('DELETE')
                                @can('delete', ${{ varName }})
                                    <button type="submit"
                                        class="px-3 py-1.5 text-sm bg-red-500 text-white rounded hover:bg-red-600 transition">
                                        Supprimer
                                    </button>
                                @endcan
                            </form>
                        </div>

                    </div>
                @endforeach
            </div>
        @else
            <p class="text-gray-500 text-center">
                Aucun {{ varName }} disponible.
            </p>
        @endif

    </div>
@endsection
